# Proj19 audit

Two passes: the automated test suites, and a live run against the real dev store `jhurald05` through a local n8n instance (2.39.8). Every row is a real result, quoted from the actual output.

## Automated tests

| # | Suite | Result | Notes |
|---|---|---|---|
| 1 | `node --test "tests/*.test.js"` | Pass | 72 tests across 10 modules: the query planner, SKU collection and SKU-to-order-name mapping, the GraphQL queries/mutations, injection-safe lookup strings, exact-SKU stock picking, the update planner and chunking, the cursor-advance rule, the GraphQL error guard and the `userErrors` guard, webhook-registration need-detection, `StockLog` row building, thank-you planning and its fixed email template, and the test-order tool. |
| 2 | `py -X utf8 -m unittest tests.test_exports` | Pass | 11 tests on the n8n export generator: the committed workflow JSON is byte-identical to what `build_exports.build()` produces, node types, the full wiring chain including the webhook self-heal and thank-you branches and both `IF` nodes on each, the cursor written in exactly one node (`Advance cursor`), `Dispatch refresh`/`Advance cursor` marked `executeOnce`, named credentials with no inline secrets (including the Gmail node using `Gmail account` with no literal address), no secret-shaped strings anywhere in the export, every Code node self-contained and passing `node --check`, the workflow saved inactive, and `Airtable update` using `PATCH` while every other HTTP node uses `POST`. |
| 3 | `py -X utf8 -m unittest tests.test_make_blueprint` | Pass | 5 tests on the Make blueprint (Task 6, extended 2026-09-21): it parses, has the webhook trigger and the GitHub dispatch call, the GitHub token is a datastore reference (`{{7.value}}`) never a literal, module 8 carries an automatic-retry `onerror` directive (added this session, see the Plan 2 live run below), and no secret-shaped string appears anywhere in the export. |

## Live run on the dev store (2026-09-20/21)

Run against `jhurald05` through a local n8n instance I started myself (`n8n-local`, existing Proj16 install), with the Shopify client-credentials app from Proj18, a fine-grained GitHub token (Actions: write only, confirmed sufficient), and n8n credentials created through its public API. Times are the actual `startedAt` from n8n's execution records; execution ids and GitHub run ids are real.

| # | Step | Result | Notes |
|---|---|---|---|
| 1 | Pre-build doc checks (shopify.dev, GitHub docs, Airtable pricing) | Pass | Confirmed all load-bearing assumptions: `orderCreate` input types and `inventoryBehaviour` enum values, `productVariants` search syntax and `inventoryQuantity`/`inventoryItem.tracked` fields, `orders` search syntax and `UPDATED_AT` sort key, GitHub's fine-grained-token permission table (Actions: write alone suffices for workflow dispatch), and Airtable's free-plan API access surviving the trial. One doc discrepancy noted, not code-affecting: GitHub's documented dispatch success response is 200, not the 204 the spec first guessed. |
| 2 | Two dry-run ticks with no new orders (exec 46, 47) | Pass | Both `success`. `Any SKUs?` took the false branch straight to `Advance cursor`, as expected with nothing to sync. |
| 3 | First test order (`#1001`, 2x `YIRGACHE-250g-WH`), next tick (exec 48) | Fail (expected/transient) | `Any SKUs?` still took the false branch — Shopify's order search index had not yet caught up to the just-created order. Not a code bug: the cursor doesn't advance on an empty result, so the order stayed in the query window for the next tick. |
| 4 | Following tick (exec 49) | Fail (real bug found) | Order was found this time: `Plan updates` correctly computed `YIRGACHE-250g-WH` 15 -> 13. `Airtable update` failed: `Your request is invalid or could not be processed by the service`. Cause: the node used `POST`, but Airtable's batch endpoint treats a `POST` carrying record ids as an invalid create request; updating existing records needs `PATCH`. Fixed in `tools/build_exports.py`, a regression test added (`test_airtable_update_uses_patch_not_post`), export rebuilt, full suite re-run clean, committed (`de05a7a`). Cursor correctly did not advance on this failed run. |
| 5 | Re-imported workflow, second order (`#1002`, 1x same SKU), next tick (exec 51) | Fail (real bug found, different node) | `Airtable update` now succeeded (`PATCH`, `Stock: 12`, matching 15-3 for both orders). `Dispatch refresh` failed: `Authorization failed - please check your credentials`. Cause: my own (not-repo) import script matched a node's credential by *type* only; since `Airtable token` and `GitHub Actions token` are both `httpHeaderAuth`, it assigned the Airtable credential to the GitHub node too. Fixed the import script to match by the credential *name* the generator already embeds, not just its type. |
| 6 | Re-imported again, third order (`#1003`, 1x same SKU), next tick (exec 53) | Pass | Full chain ran clean: `Airtable update` -> `Stock: 11` (matching Shopify's 15-4), `Dispatch refresh` succeeded, `Advance cursor` -> `{"cursor":"2026-09-20T15:43:11.000Z","orders":3}`. Confirmed independently with `gh run list -R Ainz47/Projects`: a real `refresh-catalog` run, `workflow_dispatch`, started `2026-09-20T15:45:39Z` — matching exec 53 exactly — followed by a `pages-build-deployment` run, both `completed`/`success`. |
| 7 | Idempotency: two more ticks, no new orders (exec 54, 55) | Pass | Both `success`. Both still found the 3 orders in the 10-minute overlap window (`hasSkus: true`) but correctly took `Any updates?`'s false branch, straight to `Advance cursor` — no `Airtable update`, no `Dispatch refresh`. Confirmed via `gh run list`: no additional `refresh-catalog` run appeared. |
| 8 | Failure injection: `Airtable rows` and `Airtable update` repointed at a deliberately bad credential, next tick (exec 56) | Pass | Failed cleanly: `Airtable rows` errored with `Authorization failed - please check your credentials`; execution status `error`; `Advance cursor` did not run, so the cursor stayed at `2026-09-20T15:43:11.000Z`. |
| 9 | Error alert | Pass | Proj16's `Workflow error alert` execution count went from 3 to 4 right after (exec 57, started `2026-09-20T16:00:33.930Z`, ~2s after the failure). Its own status shows `error` too, which is Proj16's already-documented WhatsApp-unattended-reliability limitation, not a Proj19 issue — the point checked here is that the alert workflow fired at all, which it did, both for this deliberate injection and for the two real bugs above (exec 50 and exec 52 in that same alert log, matching the timestamps of steps 4 and 5). |
| 10 | Recovery: real credential restored, next tick (exec 58) | Pass | `success`, ran clean end to end. |
| 11 | Reset the demo store | Pass | `npm run seed:shopify -- --to jhurald05` (Proj18) re-published all 30 products/124 variants. Read back `YIRGACHE-250g-WH`'s `inventoryQuantity` directly from Shopify afterward: `15`, matching the fixture exactly. |
| 12 | Deactivate the workflow | Pass | `POST /workflows/{id}/deactivate` -> `active: false`. |

Not checked in this pass: more than 250 orders or more than one page of matching Airtable rows in a single tick (both are guarded in code but not exercised live), n8n restarting mid-run, two ticks overlapping in real time (5-minute spacing never let this happen live), and the temporary bad-value n8n credential created for the failure-injection check (`Airtable token (test-bad, temporary)`) was left in place — n8n's public API has no delete endpoint for credentials, so removing it needs the n8n UI. Its value pairs the word `Bearer` with the placeholder `deliberately-bad-token`, not a real secret.

## Live run, Plan 2: Make, webhook self-heal, thank-you email, StockLog, and the Proj16 link (2026-09-21)

Driven directly through the `n8n-mcp` and Make MCP servers against `jhurald05` — no manual UI clicks except two owner-only actions (enabling the Gmail API in the GCP console, and toggling each workflow's "MCP access" switch). n8n workflow id `YrF6YHaYAaUPyZBh` (`Order stock sync`, 29 nodes); Make scenario id `6350078` (`order-stock-sync`, team `3001359`).

| # | Step | Result | Notes |
|---|---|---|---|
| 1 | Re-import, credentials, vars, error workflow | Pass | The Gmail credential already existed. This n8n install is Community Edition (`Settings > Variables` paywalled) — `MAKE_WEBHOOK_URL`/`THANK_YOU_ALLOW_LIST` are baked in as literals by a local-only import helper instead; the tracked export is unaffected (see README's "Known limits"). `errorWorkflow` didn't carry over on the fresh import (as expected) — fixed via `update_workflow` (`setWorkflowSettings`, `errorWorkflow: BHscSap9QKZQruIu`). |
| 2 | Webhook self-heal, 3-run check | Pass | Create -> no-duplicate-on-recheck -> manual delete (via a direct `webhookSubscriptionDelete` GraphQL call) -> self-recreate: all verified live. One run showed `ECONNRESET: aborted` on `Create webhook` after a 66s hang; the next run's list-before-create found the subscription already present, confirming the mutation had actually succeeded server-side and the client only lost the response. Design is safe across ticks; nothing guards a node-level retry-on-fail creating a duplicate *within* the same tick after this exact failure mode. |
| 3 | Dual-path stock sync (`tools/create_test_order.js`, client-credentials, no browser OAuth) | Pass | Order #1005, Make active: Shopify inventory 6->5; Airtable already showed 5 before n8n's own tick ran (Make's write landed first); n8n's tick then correctly saw `unchanged`, no double-write. Order #1006, Make deactivated: n8n updated Airtable itself (5->4) and ran its own `Dispatch refresh` cleanly. Make auto-deactivated itself between the two orders after module 8 (GitHub dispatch) hit `BundleValidationError`; 7/8 modules ran fine each time (confirmed: the Airtable write for #1005 succeeded before module 8 failed). Root cause and fix: see the dedicated row below. |
| 4 | Thank-you email | Pass | Order #1007 (owner's own address): tagged `proj19-thanked`, Gmail send succeeded (message id `1a0c3cc3261460b6`). Orders #1005/#1006 (non-allow-listed addresses): correctly never appeared in `toSend`. Second tick on #1007: `hasToSend: false`, no duplicate email. |
| 5 | Failure injection and StockLog | Pass | Swapped `Airtable rows`/`Airtable update` to the pre-existing `Airtable token (test-bad, temporary)` credential (owner's explicit permission required and given, per this session's shared-resource gate). Confirmed a clean 401 failure at `Airtable rows`, cursor did not advance, no thank-you email went out for that tick's order. Verifying the error-alert fires needed a real production execution (`executeOnce`/manual mode never triggers `errorWorkflow`) — owner approved a brief activation, one real 5-minute schedule tick fired, then immediate deactivation. The error-alert mechanism itself fired correctly (`Workflow error alert`, correctly linked, correct workflow/node/message) — but the alert's own `WhatsApp alert` node then failed to send: 401 `OAuthException` code 190 (Meta/WhatsApp Business API token expired), confirmed independently on Proj16's `Weekly lead summary`'s own `WhatsApp summary` node too (see step 6) — instance-wide, not Proj19-specific, outside this task to fix. Restored the real `Airtable token` credential; confirmed recovery with one more clean run. |
| 6 | Proj16's weekly summary reads StockLog | Pass (after a redeploy this session) | First run of this step found the *live* `Weekly lead summary` workflow (id `8BlCB7N9wC5NrraQ`) was still the pre-Plan-2 version — 6 nodes, no `Read stock log`, the old 3-line prompt — even though the code implementing this (`Proj16_n8n_Lead_Capture/code/weekly.js`'s `summarizeStock`, and the generator) had already been written, tested and committed (`dcf2133`). The live n8n instance had simply never been re-imported with that export; this was a deployment gap, not a missing-code gap. Fixed live via `update_workflow`: added the `Read stock log` node (same spreadsheet, `StockLog` tab, `Google Sheets account` credential, `alwaysOutputData`), rewired `Read leads -> Read stock log -> Summarize week`, replaced `Summarize week`'s code with the committed version (`summarize` + `summarizeStock`), and updated the Gemini prompt to the four-line version — now byte-identical to the tracked `weekly-summary.workflow.json`. Ran it live (execution id `79`): `Normalize LLM output` produced `"...Demo store: 160 stock updates, TARRAZU-1kg-WH newly sold-out."` as its fourth line — a genuine sold-out SKU from this session's own test writes, not a placeholder. The run's only failure was the same pre-existing WhatsApp/Meta 401 from step 5. |
| 7 | Make GitHub-dispatch `BundleValidationError`: root cause and fix | Resolved | Static config validated clean (`validate_module_configuration`) both in the earlier session and again here — the blueprint's module 8 config itself was never wrong. With the owner's permission, briefly activated the scenario and replayed both of the two prior failed executions (`2003e8399ff34d5bb8b62f54d65e0e7d`, `2c5e4aec84694d52a17985b1446f0f1a`) with their exact original trigger data, unchanged: both replays **succeeded** (`a5801e8ce71d4be8b8e82411c8994721`, `c1605afdf22a4701bd31be3551d1401c`). Same data, same config, different outcome — confirms an intermittent upstream/network blip on the GitHub call, not a structural defect (the same class of finding as step 2's `ECONNRESET`). Deactivated the scenario immediately after the two replays. Hardened it: added a `builtin:Break` (Retry, 3 attempts, 15 minutes apart) `onerror` handler on module 8 via `scenarios_update`; the scenario's own `isinvalid` flag flipped from `true` to `false` as a result. Re-exported and committed the blueprint with the new `onerror` block, and added `test_the_github_dispatch_call_retries_on_transient_failure` to lock it in. |
| 8 | Reset the demo store and deactivate everything | Pass | Did **not** re-run `npm run seed:airtable` — it's create-only (no update path); re-running on a non-empty table would duplicate every SKU. Ran `npm run seed:shopify -- --to jhurald05` (Proj18) instead — upserts by handle, restored the fixture baseline (30 products, 124 variants). Ran one final n8n tick to sync Airtable back to match. n8n workflow: inactive. Make scenario: inactive. |

Not checked: an order with more than one line item through the Make fast path (see README's "Not done"); refreshing the WhatsApp/Meta token (owner action, outside this task).
